# Container images for the elastic stack, mirrored to Gov ACR by scripts/mirror-images.sh.
# The path after the registry must be preserved: ECK's --container-registry flag builds
# image refs as <registry>/elasticsearch/elasticsearch:<ver>, <registry>/kibana/kibana:<ver>, etc.
# Keep versions in sync with ECK_VERSION / STACK_VERSION in service.conf.
docker.elastic.co/eck/eck-operator:3.5.0
docker.elastic.co/elasticsearch/elasticsearch:9.5.4
docker.elastic.co/kibana/kibana:9.5.4
docker.elastic.co/beats/metricbeat:9.5.4
docker.elastic.co/beats/heartbeat:9.5.4
