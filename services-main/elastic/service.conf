# Env config consumed by scripts/deploy.sh
SERVICE_NAME=elastic
NAMESPACE=elastic
ES_RELEASE_NAME=elasticsearch
KIBANA_RELEASE_NAME=kibana

# Set once the deployment method is chosen (ECK operator vs upstream Helm charts).
# For Helm: OCI refs in the Gov ACR, mirrored per charts.txt.
# ES_CHART_REF=oci://${ACR_NAME}.azurecr.us/helm/elasticsearch
# KIBANA_CHART_REF=oci://${ACR_NAME}.azurecr.us/helm/kibana
# CHART_VERSION=

NEEDS_STORAGE=true
